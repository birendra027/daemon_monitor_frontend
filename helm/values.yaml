resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
replicaCount: 1
image:
  repository: birendramondal/daemon_monitor
  tag: frontend_v-22
  pullPolicy: IfNotPresent
service:
  type: NodePort
  port: 80
api:
  # Internal cluster URL of backend service (namespace: demon-monitor-backend)
  # Adjust port if backend service exposes a different one
  baseUrl: "http://flask-backend.demon-monitor-backend.svc.cluster.local:5000"
